#ifndef TYPES_H
#define TYPES_H

#include <cstdint>

// Integer types
using MPE_INT8   = std::int8_t;
using MPE_UINT8  = std::uint8_t;
using MPE_INT16  = std::int16_t;
using MPE_UINT16 = std::uint16_t;
using MPE_INT32  = std::int32_t;
using MPE_UINT32 = std::uint32_t;
using MPE_INT64  = std::int64_t;
using MPE_UINT64 = std::uint64_t;

// Floating-point types
using MPE_FLOAT  = float;
using MPE_DOUBLE = double;

// Boolean type
using MPE_BOOLEAN = bool;

// Character types
using MPE_CHAR   = char;
using MPE_UCHAR  = unsigned char;

// Size type
using MPE_SIZE_T = std::size_t;

#endif